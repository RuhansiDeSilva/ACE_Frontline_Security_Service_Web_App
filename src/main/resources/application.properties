spring.application.name=Ace-Front-Line-Security-Solutions

server.port=8090
#Database
spring.datasource.url=jdbc:mysql://localhost:3306/security_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&defaultAuthenticationPlugin=mysql_native_password
spring.datasource.username=root
spring.datasource.password=2004Ruhansi@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.security.user.name=admin
spring.security.user.password=123

#sending emails
# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=acefrontlines@gmail.com
spring.mail.password=gimrzzkngsteaqlg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# JWT authentication
jwt.secret=AceFrontLineSecuritySolutions2026SecretKeyMustBeAtLeast256BitsLong
jwt.expiration=86400000

# Frontend portal URL used in email CTA links
app.portal.url=http://localhost:5173

logging.level.org.springframework.security=TRACE

# Automation schedules
app.jobs.timezone=Asia/Colombo
app.jobs.payment-reminders-cron=0 0 9 * * *
app.jobs.contracts-cron=0 30 9 * * *

# Strategy Configuration (project or source)
# Options: "project" (default) or "source"
app.strategy.salary=project
app.strategy.payment=project
app.strategy.billing=project

# File upload paths
app.file.upload-dir=uploads
app.upload.dir=uploads
app.upload.cv-dir=uploads/cv
app.upload.payment-dir=uploads/payment-proofs

# File upload size limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Machine Learning Service
ml.service.url=http://localhost:8000
